% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{meow_long}
\alias{meow_long}
\title{Convert the matrix simulation state to a long data frame of responses.}
\usage{
meow_long(R, admin)
}
\arguments{
\item{R}{A respondent-by-item matrix of (potential) responses.}

\item{admin}{An administration matrix the same shape as \code{R}. Non-zero entries
indicate administered items; positive integer entries additionally encode
the order of administration.}
}
\value{
A long-form data frame with columns \code{id}, \code{item}, and \code{resp}
containing only the administered responses.
}
\description{
\code{meow()} represents responses as a respondent-by-item matrix (\code{R}) together
with an administration matrix (\code{admin}). This helper returns the administered
responses as a long data frame with columns \code{id}, \code{item}, and \code{resp}, ordered
by respondent and then by the order in which items were administered. It is
the recommended bridge for module authors who prefer to work with
tidyverse-style long data inside their own item selection or parameter update
functions.
}
\examples{
R <- matrix(c(1, 0, 1, 1), nrow = 2)
admin <- matrix(c(1L, 0L, 2L, 1L), nrow = 2)
meow_long(R, admin)

}
